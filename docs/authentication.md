# Authentication Plan

This document defines the authentication work we are starting next, aligned with the current project structure (`app` for routing, `components` for shared UI, `features` for domain logic).

